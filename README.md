# A simple program that calculates days alive and time spent sleeping

- Input: User's birthday and current date
- Output: Days alive and hours slept
- Assumes an average of 8hours of sleep per day
- Has a GUI, works on NetBeans, not tested on VS Code
